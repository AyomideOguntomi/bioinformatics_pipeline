# ont_nextflow_pipeline
Nextflow pipeline for ONT data analysis

Simple Nextflow pipeline to map Oxford Nanopore reads to a reference using `minimap2` and `samtools`. The pipeline runs processes inside a Docker image defined in `Dockerfile`.
